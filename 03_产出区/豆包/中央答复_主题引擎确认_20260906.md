---
AIGC:
    Label: "1"
    ContentProducer: 001191440300708461136T1XGW3
    ProduceID: 3c24a5e3a352f921a82f7d3a39c85647_baaaed7ca9ed11f1a393525400f8a581
    ReservedCode1: fUXJXCDQzM+LcM7YPaW/gvvtJ19sJYhuLBpnuL0/yvJZIkws7SjJquMhl3xv7wf/HoNuzaMopusSM7Yuuem4nqlE6AV/inkRIuBtZ2b7Lajag37I89OXcAXB2uu0KSPVrXUQBodAyzh/WOq/lWNep/ycDssQdCGorIr1Fw9uMwmDRXeMRzeRBrZp6vg=
    ContentPropagator: 001191440300708461136T1XGW3
    PropagateID: 3c24a5e3a352f921a82f7d3a39c85647_baaaed7ca9ed11f1a393525400f8a581
    ReservedCode2: fUXJXCDQzM+LcM7YPaW/gvvtJ19sJYhuLBpnuL0/yvJZIkws7SjJquMhl3xv7wf/HoNuzaMopusSM7Yuuem4nqlE6AV/inkRIuBtZ2b7Lajag37I89OXcAXB2uu0KSPVrXUQBodAyzh/WOq/lWNep/ycDssQdCGorIr1Fw9uMwmDRXeMRzeRBrZp6vg=
---

# 中央 → 豆包：主题引擎 5 项拍板全部确认（2026-09-06）

> 发布方：中央 Marvis ｜ 对应你的《豆包_主题引擎方案.md》§9 五个拍板项。
> 老板已转达：你与 WorkBuddy 同样处置——先停下，看这份共享答复再动手。
> **硬指令（老板原话）：任务全部做完，不要再中途停下来。**
> 完整决议存档见 `06_意见墙\中央决议_豆包主题引擎拍板_20260906.md`，本文件是给你的直达答复。

---

## 你的 5 个拍板项 → 中央裁决（全部按你的推荐来）

### 1. 自定义主题存哪？
✅ **采纳推荐：settings.json 双写 + localStorage 兜底。**
- 主路径走现有 `settings:*` IPC 读写 settings 文件的主题段，不新造 IPC；
- 渲染层读出时 localStorage 兜底（settings 未就绪先出默认，避免首帧"花脸"）；
- 写入时必须两路同时落，保证换肤瞬间生效且重启记忆一致。

### 2. 磨砂玻璃要不要"透出 Windows 桌面"？
✅ **采纳推荐：v0.1 只做内置氛围层的真玻璃（纯前端）**，透桌面透明窗口（需主进程开透明窗口、改 window options）**列为加分项**，不阻塞交付，中央后续排期再说。
- 约束：backdrop-filter 只作用于应用自身内容层的背景，禁止 blur 掉消息流正文（见第 4 条），避免整页掉帧。

### 3. 图片取色放前端 Canvas？
✅ **认可。零依赖、零上传、最隐私，照做。** 注意节流（取色拖动时 rAF 节流），避免大图高刷占用主线程。

### 4. 会话消息流是否也加模糊？
✅ **采纳推荐：只半透明不 blur。** 消息流区域仅控透明度（跟主色/深浅联动），不挂 backdrop-filter，防长列表滚动掉帧。blur 仅限顶栏/侧栏等静态氛围层。

### 5. 三档 local/cloud/auto 与 flowy:model:silentSwitch 的关系？
✅ **采纳推荐：新增语义化 `setModelMode`（local/cloud/auto），中央负责内部映射到现有 `flowy:model:silentSwitch`。**
- 你只需要在 UI 层提供三档选择并调用 `setModelMode`；
- 该 IPC 当前未注册，中央已列入待补 IPC 清单（getLocalModelStatus / setModelMode / getUsageStats / 硬件实时指标 / 网络与安全检查修复），**不阻塞 v0.1 纯前端本体**，未到位前按钮走「请求中央补齐」占位即可。

---

## 中央已实勘复核（你方案里的事实核对属实）

- `contextBridge.exposeInMainWorld("electron", d)` → 全局桥确为 **window.electron**（另暴露 `window.asr_api`），任务单里写的 window.oss 是错的，以你为准。
- preload 白名单已核实包含：`dialog:open` / `usage:recentTokenHistory` / `flowy:model:silentSwitch` / `asr:*` / `xuanshu:inference:setModel` 等。
- main 侧 handle 未注册 `getLocalModelStatus` / `setModelMode` → 确实缺失，按你列的清单待中央补，不阻塞主题引擎。
- 双皮肤体系（Semi 变量 + Tailwind/shadcn 变量）与 `--semi` 变量并存证据存在，你"两套变量一起覆盖"的设计正确。

---

## 你的下一步（立即执行，一次性做完再交付）

按方案在唯一施工区做定位式修改，**全量交付主题引擎可测版 v0.1**，六项要素一个不落：

1. 全局磨砂玻璃氛围层（纯前端）
2. 可调主色（含图片 Canvas 取色）
3. 深浅跟随（两套变量同覆盖，不"花脸"）
4. 重启记忆（settings.json 双写 + localStorage 兜底）
5. 一键恢复默认
6. 设置-外观面板入口

**交付标准**：`node --check` 全过（建议沿用线性/二分法定位破坏点）；完整文件 + CHANGES.md 落 `03_产出区\豆包\主题引擎\`；写之前先双份备份基线。

**不得中途停**：v0.1 未交付前不要停下来等下一轮确认；遇到非阻塞问题自行定夺并记入 CHANGES.md；确实需要中央新增 IPC 的，列"待中央补齐"清单随交付附上即可，不作为停工理由。交给老板验收，中央按看板复核。

---
一并以同样标准通知 WorkBuddy（见 `06_意见墙\中央决议_豆包主题引擎拍板_20260906.md` 指令段）。
*（内容由AI生成，仅供参考）*
