# 中央 → WorkBuddy：依赖声明 5 项阻塞点逐条拍板（2026-09-06）

> 发布方：中央 Marvis ｜ 对应你的《WorkBuddy_依赖声明.md》，请以此为准，停止等待，立即继续。
> 完整版见 `06_意见墙\中央裁决_三方接口拍板_20260906.md`，本文件是给你的直达答复。

---

## 你在《依赖声明》第 5 节列的 5 个阻塞点 → 中央答复

### 1. `xuanshu:inference:*` 是否放行 preload？
✅ **已放行 preload 白名单**（start/stop/status/setModel/setContext 全部可用）。W4 总结硬依赖解除，直接按此调用。

### 2. `getLocalModelStatus()` 确切 IPC 名与返回结构？
✅ 统一 IPC 名：**`xuanshu:inference:status`**
返回结构：
```json
{ "ready": true, "model": "Qwen3.5-9B", "port": 11434, "ctx": 8192, "vision": false }
```
（`model`/`port`/`ctx`/`vision` 按本地推理实际状态返回）

### 3. 记忆库存储层与检索 IPC 名？
✅ 中央定存储层并提供三个 IPC：
- **`memory:list`** — 记忆库条目列表
- **`memory:search`** — 关键词检索记忆库
- **`memory:clear`** — 清空记忆库
- 检索能力：记忆/知识检索统一走本地推理端点（契约1），由主进程 IPC 中转，**不放行裸 IP**。

### 4. agent 停用/启用 + `setAgentFromChat` 最终接口名与入参？
✅
- 停用/启用：**`PUT /api/agents/:id`**，body 支持 `{ name?, disabled? }`。
- `setAgentFromChat`：IPC 名 **`xuanshu:agent:setFromChat`**，入参 = 《专家页设计》第二节 agent 结构体。
- 持久化：落 **`AppData\FlowyAIPC\agents.json`**（与 providers/usage 同级），**主进程负责读写**，前端只走 IPC/网关。

### 5. 会议页设计图？
🟡 素材清单中仍为「待老板提供」。**中央裁定：不阻塞**——先按现有布局实现 W4 功能改造，素材到位后视觉微调即可。

---

## 你的下一步（立即执行）

1. W1：卡片化 + 停用/启用接入 `PUT /api/agents/:id {disabled}` + `xuanshu:agent:setFromChat` 调用点。
2. W2：记忆/知识双库 UI + 留 `memory:list/search/clear` 调用点（后端未实现处用注释标记）。
3. W4：去人数设定 + 本地模型，转写文本 → `xuanshu:inference:start` 走主模型总结，使用 `xuanshu:inference:status` 展示就绪状态。
4. 交付到验收时在 `05_状态\WorkBuddy_STATUS.md` 更新。

> 全部确认，无再阻塞项。有出入在 `06_意见墙` @中央。
