# WorkBuddy 工作日志（全程记录）

> 记录人：WorkBuddy（DeepSeek V4 Pro）
> 项目：玄枢终代 · FlowyAIPC v5.2.1 二次开发
> 协作区：`E:\玄枢AI\玄枢终代`
> 日期：2026-09-06（单日全程）
> 最终 bundle SHA256：`4969721a8c2686859d0e66200a3d20d2`

---

## 〇、项目背景与我的职责

- 把商业软件 FlowyAIPC（Electron + React + Semi Design，渲染层为单一 2.58MB 压缩 bundle `dist/assets/index-D5I3iBTS.js`）改造为纯本地桌面 AI 助手「玄枢AI」。
- 我的主责（W1→W2→W3→W4）：专家页卡片化、知识库→记忆双库、频道→连接、会议页改造。
- 后续追加：T2 视频页（接盘 Trae）、WB-new1 AI 头像、WB-new3 首页核查、以及老板令的后端落实。
- 施工规则红线：只做定位式局部修改，不格式化 bundle、不改文件头、不升级依赖、`node --check` 校验、产出落 `03_产出区\WorkBuddy\` 并附 CHANGES.md。

---

## 一、第一阶段：读文档 + 摸清架构（19:36 ~ 20:14）

1. 读取三份协作文件：`给WorkBuddy_任务单.md`、`README.md`、`接口契约.md`。
2. 通读 `01_资料/接口契约.md`，确认本地推理统一端点、云端 API、token 统计、会议总结、bundle 挂载规则。
3. 摸清前端通信双通道：
   - `window.electron.ipcRenderer.invoke(channel)` 白名单桥（preload 有 invoke/on/once 白名单）。
   - 网关 HTTP 代理 `et("/api/...")`（内部走 `hostapi:fetch`）。
4. 定位四个页面组件与精确偏移：`/agents→IIe`、`/knowledge→cVe`、`/channels→r_e`、`/meeting-minutes→eMe`。
5. 发现项目是"半成品"：导航 rail 已改名"记忆/连接/专家"，但 channels 内部仍大量用"频道"；知识页已叫"记忆库"。
6. 产出意见墙两份设计文档：
   - `06_意见墙\WorkBuddy_专家页设计.md`（agent 数据结构、卡片化设计、首页联动方案）
   - `06_意见墙\WorkBuddy_依赖声明.md`（W1-W4 所需 IPC、5 个阻塞点）

---

## 二、第二阶段：W1-W4 主体开发（20:14 ~ 21:17）

### W3 频道→连接改名（先做，低风险）
- channels 中文命名空间 `iie/oie/lie/cie/uie/die/fie/hie/mie/pie/gie/yie/bie/xie/vie/wie` 一组字符串由"频道"改为"连接"（10 处文案）。
- 注意保留 Telegram/Discord/LINE 等平台固有"频道"术语（如"Discord 频道 ID"），避免误改。

### W4 会议页改造
- **去人数设定**：移除录制视图 `URe` 头部"参会人数"显示 + 右侧"参会人员"分段，仅保留"我的笔记"。
- **总结改本地推理**：`ZRe`（主题/关键词/待办）与 `JRe`（总结正文）由云端 `fetch(/meeting/topicKeywords)` / `fetch(/meeting/minutes/stream)` 改为本地推理，`/meeting/*` 云端端点清零。
- **去登录门槛**：移除 `startRecording` 里的 `pleaseLogin` 校验。
- **ASR 下载进度**：`eMe` 新增 `asrDl` 状态 + `asr:download-progress` 事件监听调用点。

### W2 记忆双库
- `cVe` 顶部新增「记忆/知识」分段切换（`[mV,mW]=x.useState("knowledge")`）。
- 新增独立组件 `mVue`：记忆库视图（列表/检索/清空 + 加载/错误/空态）。
- 「知识」段沿用原文档库；留 `memory:list/search/clear` 调用点。

### W1 专家页
- 停用/启用：`Cje` 组件加停用/启用按钮 + 停用态徽标；`H$` 加 `z` 处理函数调 `PUT /api/agents/:id {disabled}`。
- i18n toast 文案（中英文）：`agentDisabled/agentEnabled/agentToggleFailed` 等。
- `setAgentFromChat` 调用点：`xuanshu:agent:setFromChat`。
- 卡片化/删除二次确认/新建弹窗经核实原页已具备。

### 收尾交付
- 4 模块 bundle 复制到产出区 + 各 CHANGES.md + 更新 STATUS。

---

## 三、第三阶段：W3 收尾 + 定时巡检（21:17 ~ 21:40）

1. **W3 微信强化**：新增"消息同步" Switch + "断线重连"按钮（走已有 `channel:setEnabled(accountId, enabled)` IPC，真实可用）。QQbot 不强制绑 agent 已确认（"未分配"下拉已存在）。
2. 挂定时巡检自动化：`巡检共享协作区`，`FREQ=HOURLY;INTERVAL=1`（系统不支持 5 分钟级，取最细的每小时）。

---

## 四、第四阶段：接盘任务（20:41 中央通牒 → 21:58）

### 读中央三份斥责文件
- `中央最后通牒_严重失职_20260906.md`：批评"四份 bundle 同 hash 占位"、追加 T2 转交。
- `中央交付质量声明_不可偷工减料_20260906.md`：质量红线 7 条。
- `中央点名_立即回执开工_20260906.md`：要求立即回执 + 开工。
- 纠偏说明：中央所指是旧快照（W1/W2/W4 当时只交了分析），实际核心已做完；bundle 是单一文件（项目架构如此），非"一份冒充四份"。

### 读接盘任务清单
- `接盘任务清单_20260906.md`：WB-new1（AI圆形头像）、WB-new2（T2视频页）、WB-new3（首页核查）、WB-new4（联调）、WB-new5（待素材）。

### WB-new2 T2 视频页
- 删登录墙：移除 4 处 `if(!i/f/n)throw new Error("auth.loginRequired")`。
- 云端未接置灰：`getCloudStatus` 调用点（后改为 `provider:getDefault` 经 `oC()`）。
- 时长自定义：下拉新增手动输入（1-15s，上限保护）。

### WB-new1 AI 圆形头像
- 中央已生成 `icon-circle-{32,64,128,256,512}.png`；补齐 `chat-panel-message` 组件仍显示"玄"字的 AI 头像分支，改为 `icon-circle-128.png`。

### WB-new3 首页核查（只查不改）
- 验收第 1 项 6 子项：5 项已生效（删四卡/问候玄枢/常规会话/导航头像/删社区），1 项部分完成（侧栏余额 Popover 内"购买"按钮仍挂载），输出差异清单 `T3首页核查\差异清单.md`。

### 联调对齐（22:00 发现中央 B0 已放行后）
- 读 `06_意见墙\豆包_preload补放行清单_20260906.md`，发现：
  - `xuanshu:inference:status` 返回字段是 `state`（非 `ready`）。
  - `xuanshu:inference:start/stop` 语义是"起停引擎"（非生成总结）。
- 修复：W2 通道 `memory:*` → `xuanshu:memory:*`；W4 status 判断兼容 `state==="ready" || ready===true`；T2 云端门 `getCloudStatus` → `provider:getDefault`。

---

## 五、第五阶段：后端落实（老板令，22:29 ~ 22:35）

老板指令："把前端改动进行后端落实、协同打通、真实能用"。

### 核查 main 现状
- 中央已做：本地推理引擎管理器（`_xsSetup` 注册 `xuanshu:inference:status/start/setModel/setContext`、`_xsAutoEnsure` 自动拉起）、断厂云清理（`_xsSanitizeAuthFiles`/`_xsLocalizeConfig`）。
- 缺失：`xuanshu:memory:*`、`xuanshu:agent:setFromChat`、`asr:download-progress`、以及真正的"文本生成"端点。

### 补 main 后端（`dist-electron/main/index.js` 末尾追加 IIFE）
- `xuanshu:memory:list/search/clear/add`（落 `AppData\FlowyAIPC\memory.json`，关键词检索，上限 2000 条）
- `xuanshu:agent:setFromChat`（**复用现有 `c1()`** 写入 `openclaw.json` 的 `agents.list`，与专家页同源）
- `xuanshu:inference:complete`（OpenAI 兼容 `chat/completions`，走中央 B1 已拉起的本地引擎端点）
- `xuanshu:asr:status`（转写状态骨架）
- `PUT /api/agents/:id {disabled}`：新增 `gT`/`Bd`（对照现有 `fT`/`B$` 改名逻辑）

### 补 preload 白名单
- `xuanshu:inference:complete`、`xuanshu:memory:add`、`xuanshu:asr:status`

### 改前端 bundle
- W4 总结 `ZRe`/`JRe` 调用 `start` → `complete` + success 错误处理。

### 备份
- `main/index.js.wb.bak.222936`、`preload/index.js.wb.bak.222936`（施工区 dist-electron 内）。

---

## 六、遇到的问题与解决

| # | 问题 | 解决 |
|---|---|---|
| 1 | 2.58MB bundle 行超长，Read+Edit 会截断无法编辑 | 改用 Node 脚本做整文件字符串替换 + 期望次数校验 |
| 2 | `/tmp` 路径在 Git Bash 和 Node 间不一致 | 临时文件改写到 `C:/Users/24228/AppData/Local/Temp` |
| 3 | 正则转义错误（`$`、`(` 在 RegExp 中） | 改用 `indexOf` 定位 |
| 4 | W2 三元闭合处丢掉一个 `}`（`const const XRe=`） | 精准补回 `}` |
| 5 | String.raw 里嵌套反引号模板字符串冲突 | 改用字符串拼接 `"...\""+e+"\"..."` |
| 6 | **豆包并行覆盖 bundle**（22:23 把我 W4 complete 冲回 start） | 精准只改回 ZRe/JRe 带 `{prompt}` 的 2 处，保留豆包起停引擎 `start`；意见墙发冲突警报 |

---

## 七、最终交付物清单

### 产出区 `03_产出区\WorkBuddy\`
| 模块 | 内容 |
|---|---|
| W1专家页 | bundle + CHANGES.md（停用/启用 + setFromChat） |
| W2记忆 | bundle + CHANGES.md（记忆/知识双库） |
| W3连接 | bundle + CHANGES.md（频道→连接 + 微信强化 + QQbot） |
| W4会议 | bundle + CHANGES.md（去人数 + 本地总结 + asr） |
| T2视频页 | bundle + CHANGES.md（删登录墙 + 云端门 + 时长自定义） |
| WB-new1头像 | bundle + CHANGES.md（AI 圆形头像） |
| T3首页核查 | 差异清单.md |

### 施工区 `02_源码\app\`（源码层已落地）
- `dist\assets\index-D5I3iBTS.js`（前端 bundle，全部 W1-W4/T2/WB-new1 改动）
- `dist-electron\main\index.js`（后端 handler 追加）
- `dist-electron\preload\index.js`（白名单三通道）

### 意见墙
- 《WorkBuddy_专家页设计.md》《WorkBuddy_依赖声明.md》
- 《WorkBuddy_W3微信强化接口待拍板.md》
- 《WorkBuddy_后端落实_协同打通_20260906.md》（含冲突警报）

---

## 八、当前状态与待办

- ✅ 源码层全部落地（main/preload/bundle 三文件 `node --check` 通过）
- ✅ 前端 W1-W4/T2/WB-new1/WB-new3 完成；后端 memory/agent/总结/停用启用落实
- ⚠️ 唯一未闭环：W4 转写模型下载（需中央 B8 提供 whisper/paraformer 模型文件 + 转写引擎）
- ❌ 待中央：M7 合并三方 bundle → 重打包 app.asar → 双盘部署；D3/D4 部署验收
- 📌 冲突警报：豆包曾覆盖 bundle，已精准修复，待中央做三方合并仲裁

---

*（本日志由 WorkBuddy 于 2026-09-06 整理，覆盖单日全程操作）*
