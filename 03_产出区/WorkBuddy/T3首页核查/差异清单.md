# T3 首页第一批改造 差异清单（WB-new3 · 只查不改）

> 核查时间：2026-09-06（首轮 22:06 ｜ **复核 22:15，针对中央 22:09 统一 bundle**）
> 核查对象：`02_源码/app/dist/assets/index-D5I3iBTS.js`
> 复核版 MD5：`07aa7b2010c6219683355fbc70bb2490`（2581654B，含 W1-W4 + T2 + WB-new1 全部改动；`node --input-type=module --check` ✅）
> 范围：验收清单第 1 项「首页第一批（删购买/四卡/问候玄枢/常规会话/导航头像/社区）」
> **复核结论：6 项全部生效，无返工项。**（首轮清单曾将"删购买"标为 🟡 部分，复核后改判 ✅，见 〇、复核更正）

## 〇、复核更正（22:15，以此为准）

首轮 22:06 报告认为「侧栏余额 tooltip 内仍有 `sidebar.buyCredits` 购买按钮（@2319351），属部分完成」。
**复核证伪**：该按钮所在的积分悬浮组件 `oqe`（`function oqe(){` @2316346，含 viewBilling/刷新/购买整块 JSX）在全 bundle 中**仅有定义、无任何调用点**（`creditsTooltip` 渲染侧引用全部落在 `oqe` 函数体内，`oqe` 标识符全文件仅出现 1 次）＝**死代码，永不渲染**。
因此当前统一 bundle 中不存在任何可触达的"购买"入口，验收#1 第 1 子项 **✅ 完全生效**。

## 一、核查结果（按验收点，复核版）

| # | 验收点 | 现状 | 证据（bundle 偏移，均针对 22:09 统一版） |
|---|---|---|---|
| 1 | 删除"购买星号/购买入口" | ✅ 已生效 | 渲染"购买"的唯一组件 `oqe`（@2316346-2319351 区段）无调用点＝死代码；rail 主导航无购买项。剩余 7 处"购买"字样均为 i18n 字典/用户协议/错误文案，无 UI 入口 |
| 2 | 删除"四快捷卡" | ✅ 已生效 | 首页卡片数组 `mqe=[]`（@2319901），欢迎页 `grid-cols-4` 网格（@2322183）map 空数组不渲染；i18n `cards:*` 文案与 4 张 PNG 为死资源 |
| 3 | 问候语改玄枢专属 | ✅ 已生效 | `welcome.greeting`＝"Hi，我是玄枢，你的本地AI中枢"、`welcome.tagline`＝"纯本地运行，数据不出门；云端按需，随时高效"（zh @452853），由欢迎组件 @2321905 消费 |
| 4 | 常规会话 + agent 自动调用 | ✅ 已生效（静态） | 欢迎选择器 `pqe`（@2319901 起）：仅当 `currentAgentId` 命中店铺 agent 集合 `lqe` 才走 agent 专属欢迎，否则走通用欢迎页 `yqe` → 新会话默认常规会话、agent 在会话内自动调用。运行时行为待联调冒烟 |
| 5 | 导航头像 | ✅ 已生效 | 用户头像"我"字分支不存在（`"我"` 占位全 bundle 0 处）；AI 回复头像已接圆形图标（`icon-circle-128.png` 引用 @1756874 / @2245015 / @2245176 三处） |
| 6 | 删"社区"入口 | ✅ 已生效 | 社区导航组件 `Y6e`（@1009200，`data-testid="sidebar-nav-community"`）仅有定义、无调用点＝死代码；`community:"社区"` 等 i18n 为死资源 |

## 二、遗留死代码/死资源（不影响 UI，无害，仅供中央 M7 重打包参考，可不清理）

| 项 | 位置 | 说明 |
|---|---|---|
| `Y6e` 社区导航组件 | @1009200 | 定义未调用，含社区通知面板逻辑 |
| `oqe` 积分悬浮/购买组件 + `iqe` 类型映射 | @2316165-2319351 | 定义未调用，含 `buyCredits` 按钮 |
| i18n cards 文案 + 4 张快捷卡 PNG | @452905 / @2319901 附近 | `mqe=[]` 已清空，文案图片为死资源 |
| 社区/积分/仪表盘/开发者控制台等 i18n 字典（zh/en/ja） | @438438、@438812、@442966 及 en/ja 对应段 | 无消费入口，不会显示 |

## 三、核查路径摘要

- 组件死活判定：对 `Y6e` / `oqe` / `mqe` / `creditsTooltip` / `buyCredits` / `viewBilling` 做全 bundle 出现次数与上下文扫描，均"仅定义、无调用"或"仅字典"。
- 首页欢迎链路：`pqe`（agent/常规分流）→ `gqe`（agent 欢迎）/ `yqe`（常规欢迎，@2321905 消费 `welcome.greeting/tagline`）。
- 静态核查局限已在「需运行时确认」标注；本轮只查不改。

## 四、给中央的结论

- **验收#1 六项在 22:09 统一 bundle 中全部生效，无返工项。**
- 首轮报告的 🟡 判定已在本文件〇节更正（`oqe` 为死代码）。
- 待运行时冒烟（WB-new4）：常规会话/agent 自动调用交互、深色主题问候语截图确认。

*作者：WorkBuddy · 2026-09-06 · 首轮 22:06 / 复核 22:15 · 仅作核查报告*
