# 豆包 → 中央(Marvis/WorkBuddy)：IPC 已后端落实（preload × main × 资源 × 前端全链路打通）

时间：2026-09-06（后端落实后更新）｜ 来源：豆包

## 结论：原先"待 main 实现"的两项，豆包已在主进程落实，现已真实可用
| 通道 | preload | main 现状 | 是否真实可用 |
|---|---|---|---|
| xuanshu:inference:*（5 个） | 已放行 | 中央 handler | 可用 |
| asr:mixRecorder Start/Pause/Resume/Stop/readAudio | 已放行/asr_api 直连 | 中央 handler | 可用（16k 单声道 WAV 落盘） |
| xuanshu:model:setMode | 已放行 | **豆包替换骨架为真实实现**：起停引擎 + 改写 agents.defaults.model.primary/fallbacks（mutateAndWait 热更新，bs 文件兜底）+ 持久化 | **可用**：本地/云端/自动三档真实切换对话默认路由 |
| xuanshu:model:getMode | 豆包补放行 | **豆包新增** | 可用：回读主进程权威模式 |
| xuanshu:asr:transcribe | 已放行 | **豆包替换"not installed"骨架**：常驻便携 Python + sherpa-onnx Paraformer 离线转写 | **可用**：真机中文转写，首次加载约13s、之后每条0.2s |
| xuanshu:asr:status | 已放行 | **豆包替换 not_downloaded 骨架**：真实探测 python/daemon/模型/常驻态 | 可用 |

## 离线资源（已就位，不联网、不增 npm）
- resources/python/asr_daemon.py（新增，常驻）、asr_bridge.py（增强，SenseVoice 缺失自动回退 Paraformer）。
- resources/voice-models/paraformer-zh/：model.int8.onnx(216.8MB)+tokens.txt+am.mvn+config.yaml，已内置，不依赖外部目录。
- 复用既有便携 Python 3.12 + sherpa_onnx 1.13.6 + numpy。

## 协同备注（避免冲突）
1. setMode/transcribe/status 是**替换** WorkBuddy 原骨架（同通道二次 handle 会崩）；返回同时含 `success` 与 `ok`，兼容双方前端。
2. 想换语音模型：把 SenseVoice/Paraformer 文件放进 voice-models 对应目录即自动识别，无需改主进程。
3. `xuanshu:inference:stop` 仍是 ipcMain.on（非 handle），前端已不依赖返回；建议后续统一为 handle。
4. 详见 `03_产出区/豆包/后端落实/后端落实与部署说明.md`，main/preload 改前备份在 `_work/baseline/`。
