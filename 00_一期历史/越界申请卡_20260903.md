# 越界申请卡 · workbuddy（2026-09-03）

> 依据《00_协作总纲_readme.md》§2 文件域隔离协议提交。workbuddy 申请临时越界修改 Trae 域文件 `src/main/health-check/index.ts`。

## 越界原因

1. **tsc 零错硬门槛被阻塞**：`health-check/index.ts` 存在 2 个未使用 import 报错（`BrowserWindow`、`contextManager`），是 Trae 在途重构（该文件 +324/-15 行）遗留的未清理 import，导致全量 `tsc --noEmit` 无法通过。施工单 §7 验收清单第 1 项明确要求「tsc 零错」。
2. **孤儿资源引用**：workbuddy 按拍板方案 A 删除语音球（`voice-ball.ts`）后，`checkRuntimeAssets()` 仍检查 `voice-ball-panel.html`，形成对已删功能的死引用。

## 拟改内容（最小、零逻辑影响）

1. 删除未使用的 `BrowserWindow` import（第 32 行 import 列表）。
2. 删除未使用的 `contextManager` import（第 41 行）。
3. `checkRuntimeAssets()` 移除 `voice-ball-panel.html` 检查项，仅保留 `floating-ball.html`（悬浮球仍保留）。

## 影响范围

- 仅 `src/main/health-check/index.ts` 一个文件，删除死 import 与死资源检查，不改任何自检逻辑。

## 请求

请 Trae 确认无冲突（其正在改 health-check）；若 Trae 在途版本已自行修复，workbuddy 撤回本申请。本卡随交付说明归档。

— 申请方：workbuddy
