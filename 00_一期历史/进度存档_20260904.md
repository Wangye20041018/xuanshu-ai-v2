# 玄枢三方协作 · 进度存档 2026-09-04（阿木/产品负责人）

> 由 Marvis 存档。电脑电量告急，本次为临时断电前快速存档，明天从本文档续接。

---

## 一、今日重大变更：Trae 任务交接

- Trae 剩余约 600 积分、后续耗尽，无法继续接活。
- **用户拍板：Trae 的 agent 域后续工作全部由 Marvis（我）直接接手完成**，不再等 Trae 返工。
- 原"把 Trae 后续无缝接给 workbuddy"计划**作废**（用户今日新指令覆盖）。

## 二、Marvis 已接手完成（agent 域，tsc 零错误）

工程根：`E:\玄枢AI\xuanshu-ai-dev`（v12.3.0，Electron32+React18+TS5.7）

| 文件 | 内容 | 状态 |
|---|---|---|
| `src/main/agent/agent-manifests.ts` | 施工蓝本 §5 二十智能体全量清单（含 MetaConductor 总指挥），12 字段 x 20 个完整 | ✅ 新增 |
| `src/main/agent/meta-conductor.ts` | AI 总调度：AI 应用注册表 + 文件域锁 + 派发 + 回收质检，注册 ai_apps_* 五件套 | ✅ 新增 |
| `src/main/agent/tool-registry.ts` | registerAllTools 接入 registerMetaTools（动态 import） | ✅ 已接 |
| `src/main/agent/preset-agents.ts` | 新增 ensureManifestAgents()：20 智能体种子落盘（agt-mf-*） | ✅ 已加 |
| `src/main/agent/index.ts` | 新增 IPC：agent:list-manifests / get-manifest / seed-manifests、meta:apps-* 系列 | ✅ 已加 |
| `src/main/register/ipc.register.ts` | 启动链路接入 ensureManifestAgents 种子 | ✅ 已加 |
| 工具交叉校验 | manifest 引用的 22 个工具名全部与源码真实注册名匹配 | ✅ 已核 |
| §6a 语音清理 | agent/shared 域 voice/audio/tts/asr/floating/wake 零残留 | ✅ 已核 |

**关键实现细节：**
- 20 智能体用常量引用工具集（TR_SEARCH/TR_KNOWLEDGE/TR_VISION/TR_SOFTWARE/TR_SYSTEM/TR_SELF_MODIFY/TR_OPERATION/TR_META），非字面量。
- manifest id 形如 `agent-system-commander`；种子落盘 id 转 `agt-mf-<key>`（满足 agent-store ID_PATTERN）。
- personaId 统一用 'default'，使命/角色/边界注入 personaOverride.systemPromptSuffix。

## 三、待办（明天续接）

1. **health-check 两处 tsc 未用声明**：`src/main/health-check/index.ts` 第 32 行 BrowserWindow、第 41 行 contextManager 未使用（疑似 workbuddy 域存量，需确认归属再动；trap：文件顶部 import 区读出来已是干净的，需再核对）。
2. **build 验证**：`npx tsc --noEmit` 后跑一次完整构建，确认 build 通过。
3. **§16 运行时实测**：种子落盘实测（启动后检查 userData/agents 下是否生成 20 个 agt-mf-* 文件）、总调度 IPC 实测。
4. **DeepSeek V4 接入（待用户拍板）**：本地 6GB 跑 V4 Flash 不现实（实测档 2-8 tok/s），建议走云端 API 接入 DeepSeek，在 model-adapter 配 DeepSeek 云 provider。

## 四、今日对话决议（备忘）

- **DeepSeek V4 Flash 本地部署**：视频称"4060 8G 跑 40 token/s"不可信（284B 总参、权重 ~175GB，8G 物理装不下；Arc B580 12G 实测仅 2 tok/s）。结论：走云端 API，不折腾本地。

## 五、三方员工今日成果（存档）

- **workbuddy**：tandem-manager/scheduler/cloud-model-manager/health-check 等有改动；tandem-manager defaultMode 错误已修复。health-check 有 2 处 tsc 未用声明待修。
- **豆包**：第一阶段交付完成（语音 UI 下架、智能体编排入口移除、超算 renderer 侧移除、设置标签 12→9），tsc+build 双通过。遗留 Agents 编排 tab/超算残留/浏览器下载 UI/建设类 UI 待对账。
- **Marvis**：见上表。

## 六、长期盯梢任务（自动执行中）

- 定时任务「三方开发盯梢与完工验收」已配置，含完工检测 + tsc/build 零错验收 + 缺陷直接修复 + 整体理顺汇报。明天开机会自动巡检，也可手动让我继续。
