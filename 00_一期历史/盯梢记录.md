# 三方开发盯梢记录

> 每 30 分钟巡检一次，只记录事实、处置与遗留，不打扰用户；全部完成统一汇报。

## 2026-09-03 首巡（发起点：用户询问"怎么样了"）

### 状态快照
- **workbuddy**：已交《workbuddy_盘点与改造方案_v1.0.md》。发现关键事实：超算=联动模式(tandem)=本地推理引擎(tandem-manager)，不能直接删。提出方案 A/B，4 个阻塞点待拍板。
- **豆包**：已交《豆包_UI交付报告_v1.0.md》。第一阶段"删除清理类"全部落地（语音 UI 下架/智能体编排入口移除/超算 renderer 侧移除/设置重合项清理），tsc+build 双通过。遗留 4 项待对账（Agents 编排 tab、超算残留、浏览器下载 UI、建设类 UI）。
- **Trae**：未交进度同步；`src/main/agent/*` 有在途改动（preset-agents/tool-registry/types/react-loop/model-adapter/modules.register + App.tsx），豆包报告显示 model-adapter.ts 报错数实时变化（9→2），表明在途开发中。
- 项目根 `out\` 22:37 有 build 产物（三方之一跑了构建）。

### 已处置
- workbuddy 4 个阻塞点已由产品负责人替裁（见 《给workbuddy\产品负责人拍板_workbuddy_20260903.md》）：
  1. 方案 A 批准（去概念+保推理能力，严禁砍本地推理）；
  2. renderer 超算 UI 无需其写越界卡（豆包已自行移除）；
  3. TTS 选型授权自选（CosyVoice 2 / GPT-SoVITS 二选一，中文优先）；
  4. key 先自查再定，缺则不阻塞、标"待提供"。

### 待跟进
- Trae 首份交付/进度同步（重点：model-adapter.ts 的 tsc 报错是否清零、20 智能体落盘点）。
- workbuddy 按拍板开工后的首批改动与契约 IPC（浏览器下载、TTS）。
- 豆包建设类 UI 待双方 main 契约定稿后跟进。
