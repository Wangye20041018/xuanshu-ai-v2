---
AIGC:
    Label: "1"
    ContentProducer: 001191440300708461136T1XGW3
    ProduceID: 3c24a5e3a352f921a82f7d3a39c85647_1dd96931a9e811f190de525400461939
    ReservedCode1: wMdTwq8Y6sXUc9Z9YSf0IsGrsfcjf5nt0iV9VwmezAxh/H9rhcGuDE9l4/QvjHk+Lr/TwyKKLs0fHmFv/2eePJxxX1l5NOk2JA3kFq5o+DSt8RZSCLGi9+yr1mhsLQbcac2UZJSRAAQeAIiGSD6eucrhJZOSFgi0O0wR313q48byGmKRnAlTlmam+E4=
    ContentPropagator: 001191440300708461136T1XGW3
    PropagateID: 3c24a5e3a352f921a82f7d3a39c85647_1dd96931a9e811f190de525400461939
    ReservedCode2: wMdTwq8Y6sXUc9Z9YSf0IsGrsfcjf5nt0iV9VwmezAxh/H9rhcGuDE9l4/QvjHk+Lr/TwyKKLs0fHmFv/2eePJxxX1l5NOk2JA3kFq5o+DSt8RZSCLGi9+yr1mhsLQbcac2UZJSRAAQeAIiGSD6eucrhJZOSFgi0O0wR313q48byGmKRnAlTlmam+E4=
---

# 玄枢终代 · 集成任务单（Trae）

> 本文件是发给 **Trae** 的**唯一完整任务文件**。已把协作总纲、总体方案、排期、接口契约、协作规则、验收标准、模板全部整合进一份，**读完这一份即可开工，无需再读其他文件**。
> 协作区根目录：`E:\玄枢AI\玄枢终代`（源码工作区在 `02_源码\app`）。
> 注意：`00_一期历史` 里是旧版分散文件，仅存档参考，**以本文件为准**。

---

## 一、项目背景与最终目标（必读）

**玄枢终代** = 把商业软件 **FlowyAIPC v5.2.1**（Electron + React + OpenClaw 架构）改造为**纯本地、无云端依赖、内嵌本地大模型推理**的桌面 AI 助手。首页第一批 UI 改造已部署（网关 38789）；**其余 10 项 UI 改造 + 全部后端联动**在本次交付期内全部做完、部署、能实际使用。

产品定位（老板红线，最高优先级）：
- UI 语言沿用软件自身 Semi Design（CSS 变量 `--semi*` 体系）。
- 厂商云端（token-cloud / herdsman 等）**全断全删**；云端能力改由老板自定义 API（如 DeepSeek）经「模型-网关」面板接入。
- 本地推理内嵌（llama.cpp 主进程拉起），主模型 Qwopus3.5-9B-Coder-MTP。
- **前端 UI + 后端联动一起做**，完成即可用，不许只改皮。

---

## 二、协作区目录结构

```
E:\玄枢AI\玄枢终代\
├── 00_一期历史\   旧版分散文件，仅供存档参考（勿改）
├── 01_资料\       本整合任务单 / 接口契约 / 老板意见模板
├── 02_源码\
│   ├── app\       唯一施工区：当前部署版完整解包（9446 文件，203.9MB），唯一允许改代码处
│   └── resources_openclaw\  OpenClaw 运行库引用（D盘运行时，勿直接改）
├── 03_产出区\     豆包\ WorkBuddy\ Trae\   ← 各方成品按相对路径放这里
├── 04_集成区\     中央合并前的中转/合并补丁
├── 05_状态\       各参与方 STATUS + 中央 STATUS
├── 06_意见墙\     跨方意见/问答/老板意见
├── 07_部署产物\   重打包 app.asar / 便携版 / 安装包 / 验收清单
└── 08_参考素材\   设计图、图标原图等
```

---

## 三、协作规则（务必遵守）

1. **唯一施工区**：`E:\玄枢AI\玄枢终代\02_源码\app` 是唯一被允许修改代码的真实工作区，由中央用官方 asar 解包而来。
2. **产出落地（最重要，历史教训）**：只改自己职责范围内文件，改完复制到 `03_产出区\<你的名字>\<模块名>\` 下，**保持与施工区一致的相对路径**；每个模块目录内含：①修改后的完整文件；②`CHANGES.md`（改了哪、为什么、依赖哪些 IPC、需中央联调点）。**CHANGES.md 缺失的产出中央一律不合并**。成果必须被中央合并→打包→部署→跑起来才算真的干了。
3. **实时沟通**：每完成一个模块刷新 `05_状态\<你的名字>_STATUS.md`（模板见"我的职责"）。有依赖/问题/意见写到 `06_意见墙\<你的名字>_意见.md`。边做边交，不埋头干完一次性交。
4. **接口契约**：跨模块接口以《接口契约》（`01_资料\接口契约.md`）为准，中央维护。契约没定的一律先发 `06_意见墙` 由中央拍板，**禁止自行发明接口**。
5. **质量门槛（不达标直接返工）**：不升级/篡改依赖；成品 bundle 只做**定位式局部修改**，严禁全文格式化/改文件头/另起构建链；纯 JS 改动必须过 `node --check`；UI 必须 Semi Design 语言；含后端联动必须真实走通，不许做假按钮；不破坏已验收功能。
6. **红线**：不动 `.git`/密钥文件；不删老板模型文件；不在施工区外乱建目录；不把厂商 token 写进代码；不引入需联网的第三方依赖；不向厂商云发流量。

---

## 四、整体方案与排期（中央维护，供参考）

11 项需求最终以《验收清单》（`07_部署产物\验收清单.md`）逐项打勾验收。总工期约 7~9 天，阶段：T0 启动 → T1 主开发（第2~4天）→ T2 集成验收（第5~6天）→ T3 返工加固（第7~8天）→ T4 终验（第8~9天）。你的时间线在下方"我的职责"。

---

## 五、我的职责（T1 任务页→T2 AI 视频页→T3 核查）

# 任务文件 · Trae（DeepSeek V4 Pro · 0.72 倍 × 400 积分）


---

### 0. 你的角色

你是玄枢AI（FlowyAIPC 改版，Electron + React 成品 bundle）的专项工程师。只改渲染 bundle 里你职责范围的代码区：`E:\玄枢AI\玄枢终代\02_源码\app\dist\assets\index-D5I3iBTS.js`（2.58MB 单文件 React 生产包）。

⚠️ **定位式局部修改**：不全文格式化、不改文件头、不升级依赖、不碰 `package.json`、不动 `dist-electron`。改完 `node --check` 校验。产出必须落到产出区被中央合并部署才算干完。

---

### 一、你负责的任务（按顺序）

### T1. 任务页：定时任务修复（老板需求 3）—— 最先做

- 现状：任务页定时任务存在失效问题（历史判断与绑定 agent 相关）。
- 目标：**定时任务修好，能真实按计划触发执行**；**不绑定专门 agent，需要时按需调用即可**（任务执行时自动选择合适的 model/agent）。
- 要点：
  - 任务列表（名称、触发时间/周期、状态、上次执行、下次执行）；
  - 新建/编辑任务：任务内容 + 触发时间（一次性/周期）；
  - 启用/停用、删除（删除做二次确认，避免误删）；
  - 执行结果回显（成功/失败/日志）；
  - 真实触发的后端逻辑由中央主进程配合（IPC：`createTask/scheduleTask/listTasks/...`，契约按意见墙发布），你先把 UI 与调用点做对，必要时在 CHANGES.md 里写明需要中央补的接口。

### T2. AI 视频页改造（老板需求 7）

- **删除"需要登录"的强制**：页面不应再被登录墙挡住。
- **其他功能要能用**：视频生成流程走通 UI（输入、参数、生成按钮、进度、结果展示）。
- **云端模型未接入 → 相关功能置灰**：如果"云端模型 API 未连接"（状态从中央 IPC `getCloudStatus()` 拿，无则留好调用点），用到云端模型的能力按钮**置灰并提示**（"需在 设置-模型-网关 接入云端 API"），已接入则可用可点。
- **视频时长可自定义**：生成视频的时长参数开放自定义（如 3/5/10s 或手动输入，给出合理上限保护）。
- 目标是无登录、可操作、反馈明确，不造假生成结果（生成真实走后端/云端，中央提供接口；接口未就绪时先做 UI 与调用点）。

### T3. 核查项（只需确认并在状态里报告，不必改）

- 核查第 1 项"首页第一批改造"是否完全到位：左侧 agent 会话已改常规会话 + agent 在会话中自动调用、问候语是否玄枢专属、无购买星号/四快捷卡/导航头像/社区。发现遗漏 → 报告给中央，不要擅自大改。

### T4. 快捷辅助（有余力可做，做之前先报中央）

- 输入框三个按钮排版、@语云等豆包主责项之外的小碎活，等豆包主力交付后再认领，**严禁抢先乱改产生冲突**。

---

### 二、落地要求

- 产出放：`E:\玄枢AI\玄枢终代\03_产出区\Trae\T1任务页\`、`T2视频页\`，每模块一个子目录。
- 每模块内含：① 修改后的 `dist\assets\index-D5I3iBTS.js`（完整文件）；② `CHANGES.md`（改动位置、依赖 IPC、需中央联调点）。
- 每完成一项立即交付并在 `05_状态\Trae_STATUS.md` 更新，等中央部署验收反馈再做下一项。

### 三、质量标准（中央按此验收）

- T1：定时任务能真实创建、按计划触发、执行有结果回显，不依赖绑定特定 agent；
- T2：无登录墙、时长可自定义、云端未接入时对应功能置灰有提示、生成流程可操作；
- T3：核查结果如实汇报；
- 不破坏已验收 UI；`node --check` 通过；不新增依赖。

### 四、你的时间线

- 第1天：读文档 + 梳理任务页定时任务现状（改前先写 `06_意见墙\Trae_定时任务现状.md`：现在为什么失效、计划怎么改、需要中央补什么 IPC）。
- 第2~3天：T1 任务页交付。
- 第3~4天：T2 AI 视频页交付。
- 第4天：T3 核查报告。
- 第5~8天：按返工清单修复。

### 五、状态文件模板（05_状态\Trae_STATUS.md）

```
# Trae状态
- 当前阶段: T1主开发
- 已完成:
  - [T1任务页] v1 交付 @ 时间 —— 落03_产出区\Trae\T1任务页
- 进行中: T2视频页
- 阻塞/依赖: 定时任务触发后端 IPC 待中央发布
- 已发意见墙: 《定时任务现状》
- 下一步: ...
```

### 六、注意与禁忌

- 严禁：动 `dist-electron/main/index.js`、`package.json`、格式化 bundle、引入外部依赖、删除模型文件、向厂商云发流量。
- 疑问随时发 `06_意见墙`；不确认的功能点先留调用点并说明，别硬编码假实现。

---

## 六、关键原则提醒

- 素材缺失（如会议设计图）→ `06_意见墙` 向中央索取，**禁止硬编假布局/假路径**；未知键名/路径不要凭空捏造。
- 中央每日至少同步一次，阻塞超半天必须上报。
- **先做必做、后做加分**；每完成一个模块立即交付等验收，避免返工连锁。

---

## 七、文末附件

- **接口契约全文**：`01_资料\接口契约.md`（中央维护：本地推理统一端点、云端 API 接入、token 统计、会议总结、bundle 挂载规则）。
- **老板意见投递模板**：`01_资料\老板意见模板.md`。
*（内容由AI生成，仅供参考）*
